export async function getCurrentUser() {
  // Lógica para obtener el usuario actual, por ejemplo, desde la sesión o la base de datos
  // Ejemplo:
  return { id: "user-id" }; // Reemplaza con la lógica real para obtener el usuario
}
